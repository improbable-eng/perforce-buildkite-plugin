language: python
python:
  - "3.6"
os:
  - linux
before_install:
  - if [ "$TRAVIS_OS_NAME" = "linux" ]; then sudo ./ci/install_linux.sh; fi
install: 
  - pip install -U pip
  - pip install -U pylint pytest
script:
  - pylint **/*.py --errors-only && pytest
