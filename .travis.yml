language: python
python:
  - "2.7"
os:
  - linux
before_install:
  - pip install -U pip
install: 
  - ./ci/install_$TRAVIS_OS_NAME.sh
  - pip install -U pylint pytest
script:
  - pylint **/*.py --errors-only && pytest
