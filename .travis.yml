language: python
python:
  - "3.6"
os:
  - linux
  - osx
before_install:
  - pip install -U pip
install: 
  - if [ $TRAVIS_OS_NAME = linux ] || [ $TRAVIS_OS_NAME = osx ]; then ./ci/install_linux.sh; fi
  - pip install -U pylint pytest
script:
  - pylint **/*.py --errors-only && pytest
