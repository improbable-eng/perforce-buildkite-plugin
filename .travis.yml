---
# cache: pip # not enough installed via pip to justify
jobs:
  include:
    - os: linux
      language: python
      python: 3.6
    - os: osx
      osx_image: xcode12.2 # macOS 10.15.7 python ???
      
before_install:
  - pip3 install -U pip
  - python3 --version
install:
  - ./ci/install_$TRAVIS_OS_NAME.sh
  - python3 -m pip install -r ./ci/requirements.txt
  - python3 -m pip install -r ./python/requirements.txt
script:
  - ./ci/test.sh
  - ./ci/lint.sh
  - make local_run
